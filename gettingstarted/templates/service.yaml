apiVersion: v1
kind: Service
metadata:
  name: {{ include "gettingstarted.fullname" . }}
  labels:
    {{- include "gettingstarted.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gettingstarted.selectorLabels" . | nindent 4 }}
