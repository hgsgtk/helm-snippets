apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gettingstarted.fullname" . }}-test-connection"
  labels:
    {{- include "gettingstarted.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gettingstarted.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
